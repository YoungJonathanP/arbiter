# T09 partial evidence — migration dry run and synthetic evaluation

Verified 2026-09-09 on main, base HEAD
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`. **T09 is not complete.** Incoming
T07/T08 work remains uncommitted and unchanged; all 54 files in the T08 tested
source/asset/test/skill/grammar manifest still match. T09 is also uncommitted.
No commit, push, publication, external message or installed upgrade occurred.
The [incoming assignment](t09-assignment.md) is archived with rebased links.
The [current assignment](../next-task.md) remains T09; no successor exists.

## Implemented and evaluated

- `src/core/migration.ts`: pure dry-run API over explicitly selected Buffer
  sources and authored role choices. Inventories byte revisions/sections,
  hierarchy, status, owner, anchors and links. Maps old file/anchor locations to
  proposed assignment/coordination/dependency/history roles and destinations,
  retains originals in place, and emits questions, migration gates and rollback
  requirements. It has no filesystem reads, writes, recursive expansion or apply
  operation. Private input output stays private; there is no redaction claim.
- `test/migration.test.ts`: five synthetic regressions covering malformed links,
  duplicate anchors, recursive history, stale and unknown gates, competing owners,
  superseded scope, exact retained extraction bytes and complete backup restore
  including binary transaction intent. Exercises existing checkpoint tooling;
  no checkpoint/CLI/protocol behavior was changed.
- [Usage and evaluation limits](../../examples/migration/README.md). Automated
  tests and their fixture content are synthetic; employer content is outside
  this repository. T09 packet and execution index now record partial progress.

## User correction and private artifacts

During this session the user explicitly identified
`/Users/jonathanyoung/Documents/arbiter-data-2026-09-06-jonathan-young-nhw4ydwv2p`
as the **live KB**. This supersedes earlier snapshot-only identity statements.
Read-only observation: clean master, HEAD
`724775a83b2996c65487e7de897e2adc75452533`; installed protocol 0.4.7/base 0.4.6.
Source CLI doctor exits 2 for unsupported protocol. Directory identity is now
resolved; compatibility is not. The environment still points at frozen data.
Historical external observations were not reverified from external systems.

Private local artifacts are in
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/`:

- `REPORT.md`, `inventory-initial.json`, `selection.json`,
  `migration-manifest.json`: 12 overview/task metadata inventories plus four
  named decision/historical documents, 117,368 scanned bytes total. 103 anchors,
  119 old file/anchor mappings, with original paths retained. 36 selected links
  resolve, 45 need targeted inspection, 88 external URLs are unverified. Counts
  deduplicate within each source only. This is not a whole-KB reference audit.
- Specific source-backed scope, schedule, status and owner/gate conflicts are
  recorded privately. Roles are proposals, not an adopted hierarchy. No task was
  manufactured from a phase or historical document; no timestamps settled scope.
- `selected-assignment.md`: 4,657-byte planning draft for one bounded assignment.
  It carries essential constraints, owner/gates, source hashes and next actions;
  planning starts with one packet and no older handoff or sibling body. Two
  selected source sections are named for rechecks. This is not a protocol-complete
  implementation export or a fresh-agent result. The installed protocol alone is
  17,839 bytes, exceeding the normal offline ceiling.
- `contract-review.diff`: substantive differences in dashboard projection,
  review/status, journaled CAS, checkpoint replacement/history, owner/UUID and
  knowledge/evidence rules. Same-label schema files can differ too. Missing new
  schemas require actual adoption; version relabeling would not resolve this.
- `backup-before-migration/`, `backup-verification.json`,
  `restore-verification.json`: complete verified 730-file backup, including .git,
  followed by restore into an isolated temporary directory and comparison of
  every path/hash. No transaction directory exists in the source; none was
  excluded. No live mutation or live-migration rollback was performed.

## Verification and measured limits

- Full `npm test`: **107/107 passed**, typecheck included. Captured log:
  `/tmp/arbiter-t09-verified-tests.log`. Initial sandbox run had 15 localhost
  EPERM failures; the authorized outside-sandbox rerun passed.
- `npm run build` and `git diff --check`: passed. No operating skill edits by T09,
  so the skill validator was not rerun; the T08 skill hash remains unchanged.
- Explicit source CLI validate: frozen in-repo corpus **26 files, 0 errors,
  0 warnings**; fixtures **23 files, 0 errors, 1 existing raw-journal warning**.
  Expected outcomes asserted; all **50** frozen file hashes unchanged.
- All **270** supplied live content manifest hashes match; the entire **730-file**
  live directory, including Git, matches the verified backup at session end.
- Tested source/assets/tests/skill/grammar manifest: local `tested-sources.json`,
  SHA-256 `61d59d72d7e45de28ea1ff457849114e9075f3266ecf1489588d711a73c3a12c`.
  The tested revision is base HEAD plus dirty changes, not an invented commit.
- Synthetic normal continuation packet: **9,969 bytes** before and after adding
  **1,100,000 bytes** of historical instructions; growth **0 bytes**. No prior
  instructions enter the packet. The packet exceeds the 6 KiB target but fits
  the 10 KiB ceiling. An initially repetitive test draft exceeded the ceiling
  and was refused; compact authoring fixed it without truncating constraints.
- Synthetic assertions preserve the nullable contract constraint and superseded
  scope distinction; four gate/owner/conflict variants cannot be ready, and a
  changed input invalidates an old export. Backend preview still scans current
  corpus files: bounded recipient context is not a filesystem-read optimization.
- Current chaining handoff: **6,143 bytes** (within 6 KiB target). All **39**
  local documentation links checked, with task/index/partial-state agreement.
- Actual fresh-agent successful continuation, reads, missed constraints and stale
  action attempts are **not measured**. The incoming packet requires authorization
  for isolated agent execution; none was given. No subagents were launched.

## Remaining gates and next action

Obtain the separate local case-study path or explicit waiver (requested this
session; this generated review is not a substitute). Review the private contract
comparison, prepare an explicit adoption decision and select the migration working
copy before mutation. Resolve accountable owner and named scope/status/gate choices
from evidence, inspect the remaining links, then execute authorized migration and
fresh-agent exercises with actual acceptance measurements.

The verified backup is a baseline, not permission to overwrite later work. Recheck
source hashes, preserve concurrent edits and retain transaction intent. No Finance
Hub/ticket/transcript/profile or live KB files were edited. Live lifecycle capture,
regen and validation remain unavailable through this checkout because protocol
0.4.7 is unsupported; repository documents record progress. T09 remains partial.

## Continuation 2026-09-09: targeted link audit and adoption proposal

T09 remains partial. The [incoming packet](t09-assignment-20260909-2.md) was
archived uniquely with rebased links; the [current packet](../next-task.md)
retains T09 and all unresolved gates. No successor was invented.

- Rechecked origin/main/base HEAD and dirty work. All 56 previously tested file
  hashes matched on entry; prior T07/T08 changes were preserved.
- Targeted the 45 previously uninspected local destinations: 41 existing files,
  four false positives from prose status/hierarchy/version arrows. Updated
  `src/core/migration.ts` to recognize only detail-entry arrow syntax. A synthetic
  CRLF regression verifies real, missing and invalid targets remain classified.
- Updated private dry-run: 16 selected files, 117,368 bytes, 103 anchors and 119
  retained mappings; 36 selected local links plus 41 targeted file checks resolve.
  No remaining unresolved local destination in this scanner's selection. The 88
  external URLs are unverified; arbitrary prose/HTML/reference-style syntax and
  the rest of the KB were not audited. No post-migration acceptance is claimed.
- Prepared a concrete seven-file adoption proposal outside the repository,
  including the two new schemas absent from the earlier diff, exact proposed
  contents/hashes, behavior changes, proposed isolated fork and synthetic-only
  fresh-agent exercise plan. The proposal does not install a contract or select
  a target. Fork UUID must be assigned explicitly during approved adoption.
- Full `npm test`: **108/108 passed**, including typecheck; log
  `/tmp/arbiter-t09-continuation-tests-verified.log`. Initial sandbox run had the
  same 15 localhost EPERM failures; the outside-sandbox rerun passed.
  `npm run build` and `git diff --check` passed. Frozen source CLI validation
  remains 26/0/0 and 23/0/1 (existing raw journal warning).
- All 50 frozen file hashes and all 730 live/backup file hashes remain unchanged.
  The saved snapshot-before.json contains 275 matching entries; the earlier 270
  count referred to the separately supplied content manifest. Live clean master
  and unsupported doctor exit 2 were re-observed. No live writes occurred.
- Synthetic packet measurement remains 9,969 bytes, with zero growth after
  1,100,000 history bytes. Actual fresh-agent behavior remains unmeasured.
- Outgoing current packet is 6,139 UTF-8 bytes, within the 6 KiB target. Local
  documentation links (**43**) and task/index/partial-state agreement were verified.
- Tested source manifest: private `continuation-20260909-2/tested-sources.json`,
  SHA-256 `26264bc6143b3b83ef8ce686c411fca91381ace6c6eb64556dfd34a0093bcbcf`.
  This identifies base HEAD plus dirty source, not a new commit.

Current private evidence is under the previously identified review root in
`continuation-20260909-2/`: baseline/final verification, link audit, updated
migration manifest, proposed-contract directory/diff/hash manifest, and
`ADOPTION-REVIEW.md`. Prior evidence is retained unchanged. No employer content
was copied into this repository; the regression uses synthetic content.

The case-study path/waiver, adoption/target approval, accountable owner and exact
scope/status/gate resolutions, and fresh-agent authorization were requested again
with the concrete proposal. No answers arrived before capture. These remain
blocking predicates, not completed work. No migration, fresh-agent launch, live
capture/regen, external mutation, commit, push, publication or message occurred.
After the human supplies the decisions, follow the reviewed proposal and current
packet. Live lifecycle reconciliation requires its own compatible adoption.

## Continuation 2026-09-09: gate and baseline recheck

T09 remains partial. The [incoming packet](t09-assignment-20260909-3.md) was
archived uniquely with rebased links; exact incoming bytes are retained privately.
The [current packet](../next-task.md) retains T09, all start gates and no successor.

- Rechecked origin/main/base HEAD `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`.
  All 56 previously tested source hashes match. Prior uncommitted edits remain.
  This continuation changes only T09 checkpoint/evidence/index documents.
- Full `npm test`: **108/108 passed**, including typecheck, using the authorized
  outside-sandbox run for localhost tests. Log:
  `/tmp/arbiter-t09-continuation-3-tests.log`. Build and diff checks passed.
- Explicit source CLI validation passed with asserted results: frozen corpus
  26 files/0 errors/0 warnings; fixtures 23/0/1, the existing raw-journal warning.
  All 50 frozen file hashes, 275 snapshot entries and 730 live/backup hashes match;
  full live/backup file inventories have no extra files.
- All seven proposed contract before/after hashes match. The proposed migration
  fork does not exist. Live remains clean master at
  `724775a83b2996c65487e7de897e2adc75452533`, with unsupported protocol 0.4.7/base
  0.4.6; explicit live doctor again exits 2. No installed mutation occurred.
- Private evidence: `/Users/jonathanyoung/Documents/arbiter-t09-local-review/continuation-20260909-3/`
  contains baseline/final verification, CLI results, workspace hashes and exact
  incoming bytes. Prior proposal/audit and restore rehearsal remain intact.
- Prior selected-link audit and synthetic packet metrics retain their original
  limits: no whole-KB or post-migration acceptance, no current external verification,
  and no measured fresh-agent behavior. No runtime changes or fresh agents here.

The case-study path/waiver, exact adoption/target decision, owner and named
scope/status/gate resolutions, and synthetic-only agent authorization were
requested with the concrete proposal and remain unanswered at capture. These
are explicit incoming-assignment gates. No migration, adoption, live capture/regen,
external mutation, commit, push, publication or messages occurred. Next: obtain
those decisions before dependent action. Live reconciliation remains separately
gated on compatible adoption. Do not repeat completed code work to evade a gate.

## Continuation 2026-09-09: fourth session preservation check

T09 remains partial and final in the index. The [incoming packet](t09-assignment-20260909-4.md)
is archived uniquely with rebased links; exact original bytes are retained privately.
The [current handoff](../next-task.md) carries all unresolved gates forward.

- Repository origin, main and HEAD `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`
  match the incoming observations. All 56 tested-source hashes match; prior edits
  are preserved. This session changed only checkpoint/evidence/index documents.
- `npm test`: **108/108 passed**, including typecheck. The initial sandbox run
  passed 93 and failed 15 localhost tests with `listen EPERM`; the outside-sandbox
  rerun passed all 108. Verified log: `/tmp/arbiter-t09-continuation-4-tests-verified.log`.
  `npm run build` and `git diff --check` passed.
- Explicit `--data` CLI validation: frozen corpus 26/0/0, fixtures 23/0/1
  (existing raw-journal warning). All 50 frozen hashes, 275 saved snapshot entries,
  730 live hashes and 730 backup hashes match saved baselines. Live and backup
  inventories have no extra files. All seven proposal before/after hashes match.
- Live remains clean master at `724775a83b2996c65487e7de897e2adc75452533`.
  Explicit doctor exits 2 for unsupported protocol 0.4.7; base is still 0.4.6.
  The proposed fork does not exist. No migration, adoption or capture/regen occurred.
- Private verification and exact incoming bytes:
  `/Users/jonathanyoung/Documents/arbiter-t09-local-review/continuation-20260909-4/`.
  Prior proposal, link audit and restore rehearsal retain their stated limits;
  unchanged hashes do not establish post-migration acceptance or current external state.

The concrete adoption review and selected planning packet were inspected. The
case-study path/waiver, adoption/target choice, accountable owner and named scope,
delivered-revision, disclaimer and presentation-gate resolutions, and synthetic-only
fresh-agent authorization were requested together and remain unanswered at capture.
Stop dependent work at those explicit assignment gates. No fresh agents were launched;
actual continuation metrics remain unmeasured. No external project edits, messages,
commit, push or publication occurred. Live reconciliation remains gated on compatible
adoption; the repository handoff does not install a contract or grant new authority.

## Continuation 2026-09-09: authorized copy and intent review

The user resolved two previous gates: use another project already captured in
the notes as the comparison case, and copy the current KB for tests/iterations.
They requested individual contract review and examination of whether answers
match the scope of the question. No separate case-study path/waiver or copy
approval remains required. Contract adoption choices remain under review.

- Created private baseline and working copies, each verified against all 730
  current live file hashes, including Git. The prior backup also matches its
  baseline. Installed bytes remain protocol 0.4.7/base 0.4.6; copy doctor exits 2
  for unsupported protocol as expected. Copying did not install a contract.
- Six local check groups passed: exact copying, compatibility refusal, full visible
  pagination, selected refs/anchors, same-timestamp freshness, and full-byte
  restoration. A controlled source edit appeared in current discovery despite
  an unchanged timestamp; it was restored and all 730 copy hashes reverified.
  Live source and baseline hashes remain unchanged.
- In-memory projection of current data exposes a consequential first review:
  five-entry task cards reduce 18 visible entries to five plus 13 overflow.
  The comparison project leaves the card but remains reachable. All 43 visible
  nonarchived tasks are reachable over five directory pages. The user was asked
  full overview versus five plus paging; decision pending at capture. Freshness,
  overview breadth and human dashboard additions are separate directives.
- Four authored response examples distinguish recorded-state analysis from
  execution/acceptance gates. A saved explicit correction can support a scoped
  answer without assigning an implementation owner. Dates alone cannot resolve
  contradictions. A compact comparison packet preserves purpose and recorded
  exclusions. Neither these examples nor unit tests are fresh-agent metrics.
- Selected analysis covers seven files/115,030 bytes. Initial discovery used an
  overly broad search that emitted truncated output, so no bounded-session read
  claim is made. The manual comparison packet is 3,691 bytes and planning-only.

Private artifacts:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/intent-evaluation-20260909-ggl3R8/`.
Read `contract-items.md` for the eight individual behavior reviews and the first
dashboard recommendation; `question-scope.md` for scoped comparisons;
`empiricist-planning-packet.md` for the manual packet. Manifests, dashboard preview,
copy/iteration/final verification and exact incoming bytes remain private.
Reproducible local evaluator: private-root `evaluate-current-copy.mjs`.

Prior 108/108 tests and build remain applicable because tested runtime hashes
are unchanged. Explicit frozen CLI validations passed again at 26/0/0 and 23/0/1;
the fixture warning is the existing raw journal. Frozen hashes, prior proposal
hashes and unrelated workspace edits are preserved; diff/link/size checks passed.
The [incoming packet](t09-assignment-20260909-5.md) is archived with rebased links;
the [current handoff](../next-task.md) records the user's new authority and choices.

Next: continue individual review and scoped-answer iteration with the user, then
adopt reviewed candidates only in the selected evaluation target with a fresh UUID.
Full migration, capture/export/rollback acceptance and actual fresh sessions remain
pending. No live capture/regen, external project changes, agents, commit, push,
publication or messages occurred. T09 is partial and final, with no successor.

## Continuation 2026-09-10: user app direction

The user read the individual review and broadly supported most suggestions,
except visibility (item 5), and requested explanation of Plan inputs (item 6).
The new [app direction](../../user-app-direction.md) records connected people/entity
cards, structured human status/Done actions without an agent, linked journals and
quick notes, appointment entry, and agent-managed task overview/continuation.

Item 5 now distinguishes relationships, assignment and access. Item 6 is explained
as an optional Relevant sources list, separate from the checklist and artifacts.
The timestamp-based journal workflow is user-requested; per-task input-review
cutoffs and source-revision receipts are proposed refinements to avoid hiding an
unread note behind a later task update. Presentation, incorporation and deferral
remain distinct; new input does not automatically reopen a completed task.

Private `intent-evaluation-20260909-ggl3R8/contract-items.md` is updated, with exact
prior text in `contract-items-20260909.md`. The [incoming packet](t09-assignment-20260910.md)
is archived with rebased links and exact incoming bytes retained privately.
The [current packet](../next-task.md), T09/index and launch plan reflect the new
direction. General support does not certify every implementation detail or install
the earlier seven-file bundle. The new app examples are proposed acceptance cases,
not executed tests or evidence of implemented UI behavior.

Documentation-only update on main at `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`;
prior runtime edits remain intact. No runtime, schema, installed KB, evaluation-copy
or external project changes. Prior 108-test evidence remains historical; this turn
checks document links, handoff size/agreement, unchanged tested-source hashes,
frozen validation and diff cleanliness. T09 migration/fresh-session acceptance
remains partial. Next: settle revised entity/context behavior and input-review
acceptance, then amend reviewed candidates before test-target adoption.

## Agreement 2026-09-10: items 5 and 6

The user explicitly concurred with revised item 5 (connected people/entities;
relationships distinct from assignment and access) and item 6 (Relevant sources
and the explained linked-input review model). The review, app direction, task/index
and current handoff now record these as agreed behavior. Future sessions should
translate the decisions into concrete changes/tests without asking for the same
agreement again. Implementation and T09 migration/fresh-session acceptance remain
incomplete; agreement does not itself change installed data.

The [incoming packet](t09-assignment-20260910-2.md) is preserved with rebased links;
exact bytes are retained in the private review as `incoming-next-task-20260910-2.md`.
This is a documentation-only agreement capture; no runtime or KB changes.

## Continuation 2026-09-10: executable app-review candidate

The [incoming packet](t09-assignment-20260910-3.md) is archived uniquely with rebased
links; exact original bytes are retained privately. T09 remains partial and final.

- Added the [core candidate](../../../src/core/input-review.ts) and
  [twelve synthetic tests](../../../test/input-review.test.ts). Input receipts bind
  task, source, link incarnation and exact source revision. Later task updates never
  acknowledge notes. Presented/deferred remain actionable, concurrent input remains
  pending, and a changed task or competing review requires reconciliation.
- Task, source and association access are independent. Private input can be
  presented to its authorized reader; incorporation requires an audience that covers
  every target reader. Both entity/task cards can use the same filtered edges,
  without assigning ownership, access or notifications. This is an evaluation API,
  not deployed authentication or integrated renderer behavior.
- A synthetic filesystem exercise verifies the existing CAS foundation: linked
  source capture leaves task prose intact, Done retains auditable before/after
  bytes, stale writes fail, and Undo requires the current revision. No status UI,
  authenticated actor flow or checklist conflict controls are claimed.
- The [candidate design](../../app-review-candidate.md) specifies the representation,
  sharing boundary and remaining adapter/contract changes. Installed canonical
  receipt storage, incorporation writes, per-task review display, app controls and
  checkpoint intake still need implementation. The historical seven-file contract
  proposal is insufficient and remains unchanged; no incomplete bundle was installed.
- Full `npm test`: **120/120 passed**, including typecheck. Initial sandbox result
  was 105 pass/15 localhost `listen EPERM` failures; the permitted rerun passed.
  Log: `/tmp/arbiter-t09-app-review-tests-verified.log`. `npm run build` and
  `git diff --check` passed. Explicit frozen validations remain **26/0/0** and
  **23/0/1**, with only the existing raw-journal warning.
- Private evidence is in
  `/Users/jonathanyoung/Documents/arbiter-t09-local-review/app-review-20260910/`:
  entry/final preservation checks, source/workspace manifests, exact incoming
  packet and tested-source manifest. All 730 live, backup, baseline and working-copy
  file inventories/hashes remain identical; all 50 frozen file hashes and the seven
  historical proposal hashes match. Prior runtime and contract edits are preserved.
- Verified 69 local document links, exact incoming/archive rebasing and task/index
  agreement. The current packet is 6,142 UTF-8 bytes, within the 6 KiB target.
  The 59-file tested-source manifest SHA-256 is
  `cab2fb5573546fb7fad7b7bf9d90d558a5bacffa2c996001fd48f9feb09a5eaf`;
  this identifies HEAD plus uncommitted work, not a new commit.

No installed adoption, real-corpus migration, actual fresh-agent exercise, live
capture/regen, external write or commit occurred. Protocol 0.4.7/base 0.4.6 copies
remain unadopted. The new tests do not establish migration or fresh-session
acceptance. Next: canonical storage and CAS/app/checkpoint integration, compatible
revised candidates, then selected-target adoption and measured acceptance.

## Continuation 2026-09-11: durable input storage and local app actions

The [incoming packet](t09-assignment-20260911.md) is archived uniquely with rebased
links; exact incoming bytes are retained privately. T09 remains partial and final.

- Added [canonical ledger format](../../../src/core/input-storage-format.ts) and
  [persistence adapter](../../../src/core/input-storage.ts). Protocol/grammar
  candidate 0.4.15 defines append-only per-task Markdown audit events, independent
  link/relationship visibility, exact revision receipts, capture metadata and
  durable before/after intent. Base remains 0.4.14. Generic CAS writes enforce
  append-only history, explicit revisions, adopted protocol and task staging;
  validators recognize the role, and normalizers preserve its bytes.
- Sequential intent/content/completion writes retain interruption state. Receipts
  are effective only after content verification. Reconciliation never restores
  target files over external changes; interrupted incorporation leaves prose in
  place and cancels receipt intent for a fresh review. Attribution preserves prior
  task body, anchors and final pointer. Invalid event dates are refused.
- The [local app](../../../src/web/input-review.ts) now offers shared quick notes,
  explicit presented/deferred/dismissed review, Done with checklist resolution and
  exact-base Undo. The existing loopback/origin/token API guard supplies the
  local-operator capability; request-supplied actors/grants cannot enable private
  access. Pending proposals require arbitration. Agent terminal-transition staging
  remains in force under the narrow human-action protocol exception.
- Checkpoint preview includes every actionable shared source's exact bytes and
  revision inside the complete packet budget; new input requires review. Status,
  capture and export never acknowledge input. Last review is derived separately
  from updated. Restricted sources/edges/counts and audit bodies/reasons/reviewers
  remain excluded. A regression caught private edges between shared endpoints;
  stored edge visibility now protects that case independently.
- [Thirteen storage/app tests](../../../test/input-storage.test.ts) cover actual
  HTTP capture/review/Done/Undo, stale actions, opposing reviewers, source edits,
  staging, interruption, reconciliation, CAS/history, link incarnations, trusted
  access-policy revisions, restricted exports, budget refusal and old-contract
  refusal. Existing twelve pure-model tests remain. The HTTP suite exercises the
  real API and rendered controls; it is not a measured manual browser session or
  an isolated fresh-agent continuation exercise.
- Full `npm test`: **133/133 passed**, with typecheck. The initial sandbox run hit
  localhost `listen EPERM`; permitted runs exercised all HTTP tests. The final log
  is retained privately as `input-storage-20260910/npm-test-final.log`.
  `npm run build` passed. Explicit frozen validations are **26/0/0** and **23/0/1**,
  with only the existing raw-journal warning. The privacy export test deliberately
  uses a recorded 20,000-byte maximum; ordinary 10 KiB refusal is tested separately.
- [Version decision](../../v0.4.15-input-storage.md) and
  [updated candidate](../../app-review-candidate.md) distinguish implemented shared
  local controls from remaining personal authentication/policy administration,
  person/entity schemas/cards, appointment entry, attributed-incorporation preview
  and complete active-work overview reconciliation. Relationship/attributed-write
  library APIs are not claimed as full app authoring controls.

Private evidence root:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/input-storage-20260910/`.
It retains the exact new full-contract candidate, source/backup/candidate manifests,
workspace entry hashes, original incoming packet, prior candidate design, final
checks and test log. All 730 live/backup/baseline/working inventories/hashes remain
identical, all 50 frozen hashes match, and all seven historical proposal hashes
remain unchanged. New exact candidates remain unadopted and incomplete for the
full agreed app direction. Private content/manifests remain outside this repo.

No commit, push, message, external application change, installed adoption,
real-corpus migration or live capture/regen occurred. HEAD remains
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66` on main, with prior edits preserved.
The remaining gates and exact next actions are carried in the
[current packet](../next-task.md); this evidence does not mark T09 complete.

Finalization after resuming on 2026-09-12: **133/133 tests/typecheck passed again**,
build passed, frozen validations remained 26/0/0 and 23/0/1, and `git diff --check`
passed. All preservation checks were repeated successfully. The nine-file exact
candidate matches repository contract assets and remains unadopted. Verified 81
local document links, exact incoming/archive rebasing, task/index agreement and a
6,140-byte outgoing packet (within the 6 KiB target). The 63-file tested-source
manifest SHA-256 is
`d5a6cb9b2074483d1510413b41674ce30323b35735da57bfaf0a3bf85592d53f`.
Private `final-preservation.json`, `handoff-verification.json`,
`candidate-contract-manifest.json`, `cli-verification.json`, `tested-sources.json`
and `npm-test-resumed-20260912.log` record the final observations. The current
handoff and task now carry the 2026-09-12 recheck date. No remaining T09 acceptance
box was marked complete.

## Continuation 2026-09-12: personal app controls

Continued from main `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66` with prior
T07/T08/T09 edits preserved and all work uncommitted. Archived the incoming packet
with rebased links in [t09-assignment-20260912.md](t09-assignment-20260912.md).
The [new current packet](../next-task.md) retains T09 as partial, unassigned and
review-required; no successor or fresh agent was launched.

Protocol/base 0.4.16 and person schema 0.4.16 add shared and authenticated personal
task controls, person/entity cards in both relationship directions, appointment
capture/revision, exact attributed-incorporation preview/apply and complete
active-work views. Existing concrete schema versions remain unchanged. The ledger
header/pointer remain 0.4.15; extended source-edit/person-creation intent effects
require protocol 0.4.16. See the [decision](../../v0.4.16-app-controls.md).

The personal surface uses trusted owner-only host policy outside the KB, random
token hashes, loopback/Host/origin checks and the process capability. Requests
cannot choose actors or grants. New private source/entity/edge IDs are reserved
only for the authenticated creator, retaining exact policy history under CAS.
Existing grants and inheritance require explicit trusted administration. Shared
canonical task audiences remain shared even when a host policy lists fewer readers:
otherwise private incorporation could become visible through default exports.
Cross-file provisioning/content/audit is sequential; no atomicity or rollback over
concurrent changes is claimed. Both connected-card directions filter endpoints
and edges; association never assigns a task, creates an account or sends messages.

Incorporation preview is retained server-side, single-use, bound to actor/task,
and expires after 15 minutes. Apply rechecks the exact task/source/ledger/policy
basis and uses the same byte constructor as preview. Appointment corrections
preserve identity, existing prose/anchors and link incarnation; editing introduces
a pending source revision even after earlier dismissal. Structured human status,
Done/checklist resolution and exact-base Undo retain staging and intent safeguards.
Full active-work paging reaches nested and old active work, filtering before totals.

Verification:

- `npm test`: typecheck and **140/140** tests pass. Seven new synthetic tests cover
  exact incorporation, stale inputs, appointment history/review, both person-card
  directions, actual HTTP apply with forged/stale preview defenses, full active-work
  paging, authenticated private capture/cards/appointments/status/Undo, revocation,
  inheritance, mode checks and CAS-preserved policy history. Existing checkpoint,
  export, history-growth, staging and interruption tests also pass.
- The localhost sandbox initially rejected HTTP listeners; the required suite was
  rerun with escalation. Version/count assertions were updated for the added schema.
  A misplaced protocol addition temporarily enlarged checkpoint excerpts beyond
  their budget; its section placement was corrected, with no ceiling increase.
  A final privacy check added the shared-destination audience guard above.
- `npm run build` and `git diff --check` pass. Explicit frozen CLI validation remains
  **26 files / 0 errors / 0 warnings** and **23 files / 0 errors / 1 warning**;
  the latter is the existing valid raw-journal warning. All **50 frozen hashes**
  and inventories remain unchanged.
- All **730 files** in each live/backup/baseline/working-copy inventory still match
  the recorded hashes, including transaction/Git state. Historical seven-file and
  0.4.15 candidate bundles are unchanged. A new **ten-file** 0.4.16 candidate matches
  the exact repository assets. It has not been adopted and has no installed UUID.
- The outgoing handoff is **6,138 UTF-8 bytes**, under the 6 KiB target. Local links,
  archive rebasing and T09/index status agreement were checked. Full verification,
  exact candidate and source/preservation manifests are private artifacts under
  `/Users/jonathanyoung/Documents/arbiter-t09-local-review/app-controls-20260912/`.

These are synthetic library/local HTTP results, not browser automation, installed
live-app behavior or fresh-agent acceptance. The personal app exposes authorized
private revisions, but default checkpoint previews/exports still carry shared
input only. An authenticated agent continuation path that binds private input
presentation, explicit receipts, revocation and the complete context budget remains
unverified. Therefore the full candidate is not yet declared compatible for target
migration. Reconcile exact installed directives before adoption; then recheck
backups, allocate a fresh independent UUID, and verify refs/anchors/history,
capture/export and concurrent-edit-safe rollback. Fresh-session execution remains
unauthorized and unmeasured. No installed/live capture, regeneration, migration,
commit, push or external-system/message change occurred.

## Continuation 2026-09-12: authenticated input continuation

Continued on main `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`, preserving
uncommitted T07/T08/T09 work. Archived the incoming packet with rebased links in
[t09-assignment-20260912-2.md](t09-assignment-20260912-2.md). The
[current packet](../next-task.md) retains T09 as partial, unassigned and
review-required; no successor or fresh agent was launched.

Protocol/base 0.4.17 adds an authenticated personal continuation client flow to
the 0.4.16 app controls. Concrete schemas and the 0.4.15 ledger format remain
unchanged. See the [decision](../../v0.4.17-personal-continuation.md),
[implementation](../../../src/core/personal-continuation.ts) and
[synthetic tests](../../../test/personal-continuation.test.ts).

The loopback personal API authenticates the principal through the existing
owner-only host policy, obtains its process capability without loading a task page,
and retains a task/principal-bound preview. The exact packet contains the shared
canonical checkpoint, both applicable protocol excerpts and all pending revisions
accessible through source and edge grants. Canonical checkpoint inputs supply
manifest fields without duplication; all original source bytes and constraints
remain. Credentials stay in transport. Restricted task/checkpoint dependencies
fail closed; private checkpoint authoring/export is not claimed.

Export rechecks source/link, task/status, checkpoint, receipt and policy revisions;
it creates no receipt. After export, explicit per-revision decisions use the stored
CAS writer. Incomplete intent and staged proposals block preview. Successful review
consumes the session; another principal or forged packet cannot use it. Presentation
and deferral stay actionable; editing dismissed input creates new pending revisions.
Shared exports remain private-free, including after private receipt writes. These
are sequential filesystem operations, not atomicity against external administrators.

Verification:

- `npm test`: typecheck and **146/146 tests pass**. Six new tests cover the real
  authenticated HTTP preview/export/disposition flow, principal/source/edge revocation,
  forged and unobserved inputs, missing decisions, single use, concurrent human Done,
  source/receipt/link/checkpoint changes, complete Unicode budgeting, inaccessible
  identities/counts/prose, history growth, unrelated siblings, staged/incomplete
  actions, legacy protocol refusal and restricted checkpoint refusal.
- Complete synthetic personal packet: **11,842 UTF-8 bytes**, with an explicit
  **16,384-byte maximum and reason**. It includes all applicable rules. Oversized
  synthetic packet: **19,621 bytes**, refused at the ordinary ceiling; a new
  **30,000-byte** override/reason yields an exact **19,783-byte** export. The 6 KiB
  target and 10 KiB ordinary ceiling are unchanged. Added client context must also
  count. These measurements show that even small personal continuations may need
  an explicit larger budget with the current full protocol excerpts.
- `npm run build` and `git diff --check` pass. Frozen CLI validation with explicit
  `--data` remains **26 files / 0 errors / 0 warnings** and **23 / 0 / 1** (the
  existing raw-journal warning). All **50 frozen hashes/inventories** are unchanged.
- All **730 files** in each live/backup/baseline/working inventory retain their
  recorded hashes. Prior candidate bundles are unchanged. The new **ten-file**
  exact candidate matches repository contract assets and remains unadopted.
- Initial HTTP listeners failed with sandbox `EPERM`; required tests were rerun
  with escalation. Development checks exposed packet overhead and malformed
  concurrent-edit test fixtures. The final suite above preserves complete rules,
  measures only the outer packet, and uses valid concurrent source/checkpoint edits.

Private `contract-reconciliation.md` maps all eight reviewed directive outcomes;
`installed-candidate.diff` preserves the exact installed/candidate differences.
A read-only, in-memory candidate overlay on the evaluation copy reports **271 files,
54 errors and 195 warnings**, versus the installed baseline's **0 errors and
64 warnings**. All 54 added errors affect **18 legacy accomplishments** lacking
verification, Observations and Uncertainty. This is a concrete compatibility gate:
plan and test preservation-safe treatment without certifying historical claims or
normalizing away their prose/history. No candidate was installed to run this check.

Next: resolve legacy-record compatibility, selected-target refs/anchors and
checklist/proposal conflicts, and the target's checkpoint privacy scope. Only then
adopt/migrate the selected independent evaluation fork with verified backups and
a fresh UUID; verify capture/export/history and rollback without overwriting
concurrent edits. The settled comparison and items 5/6 remain settled. Recorded-state
analysis needs no implementation owner; execution needs its own evidence.

These are synthetic client/library results and static contract analysis, not browser
automation, installed app behavior or fresh-agent acceptance. Fresh-session execution
remains unauthorized/unmeasured. Live rollout/reconciliation remain separate. No
live/installed capture, regeneration, real-corpus migration, UUID allocation, commit,
push, external-system or message change occurred. Evidence, exact candidate and
preservation/source manifests remain outside this repository under
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/personal-continuation-20260912/`.

Final capture verification: **93 local links** resolve, the incoming archive has
exactly rebased links, task/index status agrees, and the outgoing packet is
**6,143 UTF-8 bytes** (within the 6 KiB target). Three recorded candidate bundles
match their manifests. The 68-file tested-source manifest SHA-256 is
`5880007f36a2b56d0b364e3e694d0cadcb7418ad7a8583a5829eba15d5ac5a5f`.
Private `final-verification.json`, `handoff-verification.json`,
`final-preservation.json`, `candidate-preflight.json`, `cli-verification.json`
and `tested-sources.json` retain the observations.

## Continuation 2026-09-12: legacy compatibility and independent fork

Continued on main `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`, preserving all
uncommitted T07/T08/T09 work. Archived the incoming packet with rebased links in
[t09-assignment-20260912-3.md](t09-assignment-20260912-3.md). The
[current packet](../next-task.md) retains T09 as partial, unassigned and
review-required; final task, no successor or fresh agent launched.

Accomplishment schema 0.4.18 makes missing verification, Observations and Uncertainty
compatible with preservation of historical claims. Missing verification warns and
never counts impact; validation retains all observed-attestation requirements,
invalid enum rejection and the legacy Evidence-link rule. New skeletons/promotions
explicitly emit unverified and both sections. Protocol/base remain 0.4.17; other
schemas/ledger bytes are unchanged. Schema resolution exposes the concrete version;
CLI identity checks retain support for installed accomplishment 0.4 and 0.4.14.
See the [decision](../../v0.4.18-legacy-accomplishments.md),
[schema](../../../assets/contract/types/accomplishment.md) and
[tests](../../../test/evidence.test.ts).

The revised in-memory candidate overlay reports **271 files / 0 errors / 213
warnings**, including 18 advisory legacy records, versus installed baseline
**0 errors / 64 warnings**. The prior candidate had 54 required-field/section
errors. All legacy accomplishment bytes remain unchanged: no normalization or
invented observation was used to pass. Private reconciliation maps all eight
reviewed directive outcomes to the scoped local migration exercise. The exact
new ten-file candidate and installed diff are retained alongside its hash manifest.

Selected-target inspection found no staged proposal, reserved checkpoint collision
or explicitly restricted input among the task and two selected documents. Shared
metadata defaults are not employer publication or multiuser access approval. The
scope remained this local filesystem operator on an independent fork; no server,
account, grant, personal ledger or external export was created. All five dashboard
cards had no nonempty stray rows; the stored dashboard remains byte-identical and
advisory. Dashboard regeneration and recovery of real handwritten additions were
not exercised.

A new independent fork and complete **730-file** pre-adoption backup were verified
before adoption. The fork received the exact contract plus a fresh UUID, using
CAS writes and re-observation. Two library checkpoint captures added only the
selected task's checkpoint pointer and updated timestamp. Its original body,
status, historical documents, **18 anchors** and **52 checked local links** remain.
External references remain unverified. The checkpoint carries the recorded
capability objective and explicit scope exclusion directly; prior successful
runs do not override that exclusion or prove accepted capability. Owner remains
unassigned/readiness review-required; no engineering work is assigned.

Verification:

- **148/148 tests and typecheck pass**. New regressions cover CRLF legacy-byte
  preservation, visible unverified claims, complete attestation metadata without
  explicit verification, strict observed fields, invalid values, retained legacy
  Evidence requirements, old-schema requirements and new candidate defaults.
  The first typecheck exposed missing version information in resolved schemas;
  this is now retained explicitly. The sandbox run then passed all non-HTTP tests
  but failed 24 localhost listeners with `EPERM`; the escalated full run passed.
- **Build and diff checks pass.** Explicit frozen CLI validations remain
  **26/0/0** and **23/0/1**, with only the existing raw-journal warning. All **50
  frozen hashes/inventories** remain unchanged.
- First complete real-corpus export: **83,364 UTF-8 bytes**. Second/current:
  **83,472 bytes**, with an explicit **131,072-byte maximum and reason** retaining
  the entire imported task and applicable rules. Ordinary preview was **83,181
  bytes** and export refused. Exact exported bytes matched preview; old preview
  refused after replacement. The prior checkpoint's **5,010 exact bytes** remain
  in immutable history. The original task file was **69,845 bytes**; its body was
  preserved, not semantically compacted. These large packets do not satisfy compact
  or fresh-session acceptance, and include no claim of agent comprehension.
- Migrated fork CLI/history validation: **273 files / 0 errors / 214 warnings**.
  Complete fork inventory: **747 files**, including transaction journals/history.
  All original accomplishment and dashboard bytes remain unchanged.
- Rollback on a separate test-owned copy restored every original path/file byte
  in the **730-file** backup, including Git and recovery files. Another test copy
  with a simulated concurrent edit refused the entire restore before writes and
  retained all observed bytes. The verified fork and backup were unchanged by
  these exercises. Sequential preflight/hash checks do not lock out arbitrary
  external writers in the check-to-write window.
- Original live/backup/baseline/working inventories each retain all **730 hashes**;
  prior candidate bundles are unchanged. All employer data, diffs, manifests,
  previews and exports remain outside this repository.

Private evidence root:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/legacy-compatibility-20260912/`.
`fork/` is the verified migrated evaluation; `fork-before/` is its exact pre-adoption
backup. `rollback-restored/` demonstrates full restoration; `rollback-concurrent/`
retains the deliberate test edit. `fork-adoption.json` records the fresh UUID;
`fork-after.json` records the final inventory. `contract-reconciliation.md`,
`candidate-preflight.json`, `migration-verification.json`, `rollback-verification.json`,
`target-review.json`, `target-reference-checks.json` and the test log retain evidence.
These are real-corpus local library and synthetic test results, not browser,
live-app or fresh-session agent measurements.

Next: preserve this fork as evidence and use another isolated iteration to review
selected Summary/checklist content into a compact independent continuation. Retain
all prose/history, refs/anchors and essential source constraints; measure complete
context and verify capture/export/rollback. Fresh-session execution remains
unauthorized/unmeasured; checkpoint at that gate unless authority arrives. Live
rollout/reconciliation remain separate. No live adoption/capture, dashboard regen,
commit, push, external-system or message change occurred.

Final capture verified 2026-09-13 (local date): **89 local links** resolve, the
incoming archive is exactly rebased, task/index agree, and the outgoing packet is
**6,135 UTF-8 bytes**, within the 6 KiB target. All four recorded candidate bundles
match their manifests. The 68-file tested-source manifest SHA-256 is
`75c651bd8490ea0d869192095c82b5dd42c096cab9ca4e37e863c4a35ba790a5`.
Private `final-verification.json`, `handoff-verification.json`,
`final-preservation.json` and `tested-sources.json` retain the observations.
The final checker was corrected to distinguish object-ref files from same-named
detail directories; all 52 target links then passed post-migration resolution.

## Continuation 2026-09-13: task compaction and whole-context gate

Continued on main `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`; all prior
uncommitted T07/T08/T09 source/contract work is preserved. Incoming assignment
archived with rebased links in [t09-assignment-20260913.md](t09-assignment-20260913.md).
The [current packet](../next-task.md) retains partial T09, final/no successor.
This continuation changes T09 documentation and private evaluation artifacts only.

A new isolated iteration has a fresh UUID and complete **747-file** backup of the
preceding verified fork. Contract prose/schemas are unchanged: protocol/base 0.4.17,
accomplishment 0.4.18. All original live/backup/baseline/working **730-file**
inventories, all **50 frozen files**, the preceding fork/backups and four candidate
bundles retain their hashes. No access grant, server, ledger, live adoption/capture
or dashboard regeneration occurred. Shared defaults imply no publication approval.

The selected imported task shrank from **69,906 to 5,424 bytes**; Summary is **593
bytes**, and each authored row stays under 400 code points. The original 69,845-byte
pre-adoption task differs only by the prior checkpoint metadata; the 69,906-byte
input here is the verified migrated fork task. All **32 checklist marks and 18
anchors** remain in place. The exact full original task, including opaque prose,
artifact URLs and detail links, is embedded in a new linked tier-3 detail. All
**52 previously checked local links** still resolve. Existing historical files,
legacy accomplishment claims and dashboard bytes are unchanged. No old target is
removed; compatible links reach the preserved originals.

The private recorded-state answer distinguishes the capability objective, delivered
milestones, explicit exclusions, predictions and unproven retained acceptance. It
resolves the old hold versus recorded landing, accepted-run versus excluded-tree
scope, specific capture-artifact correction versus separate product defects, and
numbering ambiguity through explicit source content. Marks remain unchanged even
where prose records later delivery. This is scoped analysis, not new ownership,
external verification, implementation authority or a fresh-agent reading exercise.

Verification:

- **148/148 tests plus typecheck pass.** The sandbox run passed 124 tests but
  blocked 24 localhost listeners with `EPERM`; the escalated full run passed all.
  **Build and diff checks pass.** Runtime source/contract bytes match the prior
  tested-source manifest; no new runtime regression tests were added.
- Both explicit frozen CLI validations remain **26/0/0** and **23/0/1**, with only
  the existing raw-journal warning. Hashes/inventories remain unchanged.
- Two supported CAS checkpoint captures/export checks pass; exact previewed bytes
  export, stale previews refuse, and the prior **5,118-byte** checkpoint plus new
  **4,419-byte** checkpoint versions are retained under exact digest filenames.
- Ordinary packet: **18,341 bytes**, export refused. Reviewed packet: **18,505**,
  with explicit **24,576-byte maximum/reason**. Components are task **5,424**,
  checkpoint **4,419**, complete checkpoint rules **5,602**, envelope/framing
  **3,060**. Checkpoint frontmatter alone is **2,403**: rules/frontmatter/framing
  total **11,065** before task/checkpoint body for this selected representation.
  Further Summary trimming alone cannot meet 10 KiB. This is a measured component
  breakdown, not a universal lower-bound proof for every possible renderer.
- Adding **69,778 bytes** of optional synthetic history leaves the re-previewed
  normal packet byte-identical. The old preview refuses the changed source basis;
  a fresh preview exports exactly. History growth is excluded from emitted context,
  but the library still reads corpus files for freshness/privacy. No claim of zero
  internal history reads, measured unnecessary agent reads or comprehension.
- ALL required context remains a gate: packet plus full installed protocol and
  base/task schemas is **59,146 bytes** under packet-plus-raw-files accounting;
  optional answer adds **3,427**. Complete offline export with all selected inputs
  is **71,423** under explicit **131,072 maximum/reason**. No client preamble was
  added; future client/system/tool context must also be measured. Neither total
  is an actual model-window or fresh-agent measurement. A short authored task or
  export with a larger override does not establish compact acceptance.
- Iteration CLI/library validation with history: **278 files / 0 errors / 185
  warnings**. Complete inventory: **760 files**, including retained transactions.
- Separate test-owned rollback restored every byte/path of the **747-file** backup.
  Another copy with an injected concurrent edit refused the entire restore before
  writes and retained its observed bytes. The iteration and backup stay unchanged.
  Sequential hash checks do not exclude arbitrary external editors.

Private evidence root:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/compact-continuation-20260913/`.
`iteration/` is the current evaluation; `iteration-before/` is its full backup.
`compaction-plan.json` maps each row/anchor to authored replacement and exact history.
`compact-assessment.md` records the bounded answer and scope resolutions.
`contract-reconciliation.md` documents the eight inherited directive outcomes,
explicit history-extraction change and complete-context accounting.
`compaction-verification.json`, `rollback-verification.json`, `iteration-after.json`,
`ordinary-preview.json`, exact exports and test logs retain the measured results.
All employer prose, source paths, manifests and exports remain outside the repository.

Stopped at the **whole-context budget gate**. Next: use synthetic data to review
and measure a bounded assignment/bootstrap design, retaining all essential rules,
privacy, revisions and authority checks. Do not silently truncate rules, raise the
ceiling, label KB-linked export as complete bootstrap, or repeat private migration
before the design can meet its stated budget. Fresh-session execution remains
unauthorized/unmeasured; live rollout/reconciliation are separate. T09 remains
partial; history-stable emission is verified but within-budget growth acceptance
and a measured fresh start are not marked complete. No agent, commit, push,
publication, messages or external-system change occurred.

Final capture verification: **86 local links** resolve; incoming archive is exactly
rebased; task/index agree; outgoing repository handoff file is **6,136 UTF-8 bytes**.
That file-size check does not claim the linked bootstrap fits the whole-context
budget. All protected inventories and prior tested runtime/contract bytes match.
The 68-file tested-source manifest SHA-256 remains
`75c651bd8490ea0d869192095c82b5dd42c096cab9ca4e37e863c4a35ba790a5`.
Private `final-verification.json`, `handoff-verification.json`,
`final-preservation.json` and `tested-sources.json` retain final observations.

## Continuation 2026-09-13: synthetic whole-bootstrap design gate

Assignment: test a bounded synthetic bootstrap before another real-data iteration,
preserving required rules, privacy and revisions. Incoming packet is archived with
rebased links in [t09-assignment-20260913-2.md](t09-assignment-20260913-2.md).
Main remains `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`; earlier uncommitted work is
preserved. Only a synthetic evaluator, four tests and T09 documentation are added
or changed. No runtime/contract changes; protocol/base 0.4.17, accomplishment 0.4.18.

[Reproducible design and measurement](../../bootstrap-budget-evaluation.md):
`node --import tsx scripts/evaluate-bootstrap.ts` creates only a disposable
synthetic KB. Exact offline packet 45,671 + declared client 255 + receipt 571 =
**46,497 UTF-8 bytes**, under an explicit **65,536 maximum/reason**. Raw required
sources total **43,126**; full protocol/base/task alone **40,641**. Linked packet
10,098 plus full rules gives 50,739, including a repeated 5,602-byte excerpt.
Removing every framing/duplicate metadata byte cannot satisfy 10 KiB. This is a
lower-bound result for retaining the current exact sources, not a proof that no
future compact contract could fit.

Two captures preserve exact prior checkpoint bytes; export matches reviewed bytes;
superseded previews refuse. 312,181 added synthetic history bytes leave re-previewed
packet identical. Immutable history does not stale export; ordinary optional-source
growth does. Private source bodies/identities/counts stay excluded and selection
refuses even with override. New shared input appears and requires review; export
acknowledges nothing. Changed source revision stales old previews. Required input
that grows export to 136,448 bytes refuses without truncation. Synthetic validation
has zero errors. Tests cover all missing context slots, multibyte/repeated text,
client overhead, invalid overrides and receipt-induced overflow.

**152/152 tests/typecheck pass**, including four new tests. Initial sandbox run:
128 passed, 24 localhost EPERM failures. Escalated full run: 152 passed, 0 failed.
Build and diff checks pass. Explicit frozen validations: 26/0/0 and 23/0/1, with
only the pre-existing raw-journal warning. All 50 frozen hashes, 730-file original
live/backup/baseline/working inventories, prior forks/candidates and previous
760-file compaction/747-file backup remain unchanged. Previous rollback copies
remain unchanged too. Existing tracked/untracked source work was not overwritten.

Private artifacts: `/Users/jonathanyoung/Documents/arbiter-t09-local-review/bounded-bootstrap-20260913/`:
`synthetic-report.json`, initial/escalated npm-test logs, npm-build log, both frozen
validation logs, before/final preservation inventories, tested-sources manifest,
archive/link/size verification and final-verification report. Sequential hashes
prove observed bytes only, not exclusion of arbitrary concurrent external editors.

Stopped at the [design decision](../../bootstrap-budget-evaluation.md#design-decision-gate):
retain the full contract with an explicitly accepted larger measured whole-context
maximum, or design a compact operational contract/client with a complete clause map
and versioned decision. Without a choice, the next agent may prepare that map from
bundled contracts and checkpoint for review before normative edits. Ordinary compact
acceptance remains unmet. Actual host context is unknown and refused by the audit;
synthetic zero slots are not real client measurements. No agent reading,
comprehension or unnecessary-read metric is claimed. Fresh-agent execution remains
unauthorized; live rollout/reconciliation remain separate. No new real-data copy,
live adoption/capture/regen, commit, push, publication, messages or external changes.

T09 remains partial, final and without a successor. The
[current handoff](../next-task.md) carries the design/authority gates forward.

## Continuation 2026-09-13: clause-preservation map

Completed the bounded no-choice fallback: map bundled directives to a proposed
compact operational contract, then stop for review. Incoming packet is uniquely
archived with rebased links in [t09-assignment-20260913-3.md](t09-assignment-20260913-3.md).
Main remains `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`; prior uncommitted T07/T08/T09
work is preserved. Only documentation changed; no normative edits or runtime code.
Protocol/base remain 0.4.17; accomplishment remains 0.4.18.

The [proposal](../../compact-contract-map.md) maps **81 protocol ranges and 201
schema ranges** across all **10 bundled files / 50,572 bytes**. Each nonblank source
line has a destination, including metadata, complete examples, each schema field
and section, prose and footers. Exact source hashes bind the ranges. Global rules
retain authority, navigation, privacy, preservation, writes/recovery, budget and
input/evidence duties. Required operation modules specify activation, dependencies
and stop-before-operation gates. Full original clauses remain the preservation
baseline; table labels do not replace them. Future rewritten semantic equivalence
still requires review; mechanical coverage does not establish it.

Budgeting begins with actual host/client overhead. Unknown components prevent any
numerical allocation. The unchanged source module lower bounds plus base/task
schemas already exceed 10 KiB: **22,383 shared review; 21,563 task edit; 25,937
personal review**. These exclude global rules/dispatcher/grammar/host, assignment,
input bytes and receipts. The map cannot establish compact admission. A compact
route requires reviewed rewriting as well as dispatch. The full-read obligation
and required input manifest are explicitly flagged as normative change decisions.
Other review issues preserve bare-ref ambiguity handling, existing version wording,
legacy accomplishment evidence, human terminal exception and timing thresholds.

New checks: all source lines mapped without overlaps, destinations defined, source
hashes match, schema clauses retained, module lower-bound arithmetic, manual review
of eight operation scenarios, frozen validation **26/0/0 and 23/0/1** (existing
raw-journal warning), all **50 frozen hashes and 14 protected inventories** unchanged,
exact incoming archive rebasing, local link/anchor resolution, handoff size and
T09/index consistency, and `git diff --check`. The previous **152/152 tests/typecheck
and build pass** are retained evidence: every tested source hash still matches.
No runtime suite rerun was needed for these documentation-only changes; no test
asserts this proposed dispatcher is implemented or proves agent comprehension.

Private verification artifacts:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/contract-map-20260913/`.
Before/after preservation and workspace inventories, source/coverage/module audits,
frozen validation logs and final handoff verification remain there. Protected
originals, backups, forks, candidates and prior 760-file iteration/747-file backup
are unchanged. Sequential observations do not exclude arbitrary external editors.
Prior synthetic 46,497-byte result and history/privacy/input tests remain evidence
from the preceding harness; no real-data iteration or fresh-agent exercise occurred.

**Stopped for design review.** Choose an explicitly accepted larger measured
whole-context maximum, or authorize the reviewed compact-contract/versioned design
with measured client inventory. Without a choice, the next agent checkpoints the
same gate rather than repeating the map, trimming prose or migrating. Fresh-agent
launch and live adoption/reconciliation remain separate authorizations. No live
capture/regen, commit, push, publication, messages or external-system changes.
T09 remains partial, final and without a successor; the
[current handoff](../next-task.md) carries the review gate and chaining prompt.

Final capture verification: **113 local links** resolve; incoming archive is exactly
rebased; task/index agree; outgoing handoff is **6,119 UTF-8 bytes**, within its
6 KiB file target. That file-size result is not a whole-bootstrap admission claim.
Source/schema coverage and protected inventories pass the final recheck. Private
`coverage-verification.json`, `handoff-verification.json`, `final-preservation.json`
and `final-verification.json` retain the observations.


## Continuation 2026-09-13: decision review checkpoint

Completed the assigned review and no-choice fallback. The current user request
asked to complete the incoming assignment; it supplied no budget/contract choice
or new normative, fresh-agent or live-adoption authority. T09 remains in-flight
(partial), unassigned and review-required, final with no successor. The incoming
packet is archived with exactly rebased links in
[t09-assignment-20260913-4.md](t09-assignment-20260913-4.md).

Reviewed the [map decision](../../compact-contract-map.md#review-decision), budget,
required-read dispatcher and six explicit review issues. The larger route still
needs an accepted measured whole-context maximum and acceptance-policy decision.
The compact route needs reviewed rewriting and dispatch, a versioned decision,
and measured client inventory before allocation. Full-read obligations, schema
interpretation, compound operations, existing clause tensions and unchanged policy
exceptions remain explicit. Host context is unknown. No route was selected and
no map regeneration, trimming, normative edit or migration was performed.

Verified main at `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66` and preserved earlier
uncommitted T07/T08/T09 work. The map and its 10 contract source hashes are unchanged.
All 71 tested source hashes match prior **152/152 tests/typecheck/build pass**;
those checks were not rerun for this documentation-only checkpoint. Fresh explicit
`--data` frozen validation returned **26/0/0 and 23/0/1**, retaining the existing
raw-journal warning. All **50 frozen hashes and 14 protected inventories** match
before and after. These snapshots do not establish live external-system state or
exclude arbitrary concurrent editors. No fresh-agent acceptance is claimed.

Private evidence is in
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/decision-review-20260913/`:
baseline/source/workspace/preservation inventories, exact incoming bytes, validation
logs, and final preservation/link/archive/size verification. Only the task, index,
current handoff and this append-only evidence changed, plus the new archive.
No live capture/regen, commit, push, publication, messages or external changes.
The [current handoff](../next-task.md) retains the same explicit choice gate and
required chaining prompt; a human starts the next session.

Final capture verification: **102 local links** resolve; the incoming archive is
exactly rebased, task/index agree, and the outgoing handoff is **6,139 UTF-8 bytes**
within its 6 KiB file target. Whole-session context remains unmeasured; this file
size is not admission or agent-acceptance evidence. `git diff --check` and final
source/frozen/protected preservation checks pass. Results are retained in private
`handoff-verification.json`, `final-preservation.json` and `final-verification.json`.


## Continuation 2026-09-14: progressive disclosure direction

The user clarified the intended product during budget review: a new agent follows
a task URL/path, reads a concise flight/task summary and checklist, then the next
eligible task's T3 handoff. The handoff should contain nearly everything needed
for that assignment, including essential decisions, with pointers to larger plans,
investigations, tickets, PRs and prior handoffs for relevant deeper questions.
The user seeks manageable flights and progressive disclosure, not a fixed cap on
all context loaded during a working session. No further choice between the prior
two budget routes is required.

Recorded the [accepted direction](../../progressive-disclosure-handoffs.md), updated
the design/template, T09 and execution index, and replaced the chaining packet.
The [incoming archive](t09-assignment-20260914.md) preserves its text with exactly
rebased links. The whole-bootstrap evaluation and clause map retain their original
bodies with explicit notices superseding the cumulative admission gate. The user
clarification is the authority for that resolution, not timestamp precedence.
No compact dispatcher or larger session maximum was selected. Existing packet
limits and mandatory protocol/schema/input-review duties remain intact. A flight
uses existing goal/parent-task relations; no new type or lifecycle was introduced.

Read the current protocol/grammar export wording and exporter inclusion/budget
logic: it budgets the emitted packet; the separate synthetic whole-bootstrap
helper adds declared host slots. Its prior test results remain experiment evidence,
not acceptance policy for the newly clarified user journey. Optional background
investigations remain distinct from required pending tagged-input presentation.
No runtime, normative protocol/grammar or schema changes were made.

Verified main at `bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`. All 71 tested source
hashes still match prior **152/152 tests/typecheck/build pass**; no suite rerun for
these documentation-only changes. Fresh explicit `--data` validations are
**26/0/0 and 23/0/1**, retaining the existing raw-journal warning. All **50 frozen
hashes and 14 protected inventories** remain unchanged, including prior compaction
and backups. Sequential snapshots do not prove live external-system state or
exclude arbitrary editors. No fresh agent, real-data iteration, live capture/regen,
commit, push, messages or external-system changes occurred.

Private evidence:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/progressive-disclosure-20260914/`.
Baseline/source/workspace/protected inventories, incoming bytes, validation logs
and final link/archive/size/preservation verification remain there. T09 remains
partial and unassigned, final with no successor. Next is one synthetic flight/task
continuation example and exporter/read accounting against the accepted criteria;
fresh-agent execution and live adoption/reconciliation remain separate gates.


Follow-up clarification in the same discussion: the user accepts reasonable
numeric handoff limits and also requires limits on session loading so that the
agent can accomplish the task. The decision/design/template now distinguish
packet size from a task-scoped startup allowance and working reserve. Further
reads are bounded expansions; narrow/split/checkpoint before consuming execution
and verification room. Startup/reserve numbers are not user-selected; calibration
and exhaustion behavior are part of the next synthetic exercise. This clarification
does not authorize unlimited loading or adopt 10 KiB as the entire session limit.


Final capture verification: **138 local links** resolve; the incoming archive is
exactly rebased; task/index agree; the outgoing handoff is **6,144 UTF-8 bytes**,
within the 6 KiB file target. This is a packet-file measurement, not a total-session
or working-reserve measurement. Historical proposal bodies and append-only evidence
are preserved. `git diff --check`, all 71 tested-source hashes, 50 frozen hashes
and 14 protected inventories pass. Private `handoff-verification.json`,
`final-preservation.json` and `final-verification.json` retain the results.


## Continuation 2026-09-14: synthetic flight continuation

Completed the incoming synthetic assignment at main
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66` with existing T07/T08/T09 work preserved.
The [incoming packet](t09-assignment-20260914-2.md) is archived exactly except for
rebased links. The [review artifact](../../examples/flight-continuation/README.md)
links the flight, selected task/current T3 handoff, synthetic plan/investigation/
ticket/PR pointers, exact exporter packets and measurements. The
[evaluator](../../../scripts/evaluate-flight-continuation.ts) is exercised by one
[integration test](../../../test/flight-continuation.test.ts). No runtime, contract,
schema, real implementation, private-data iteration or receiving agent is claimed.

### Observations

- Next eligibility follows current design/owner/input gates; release remains blocked.
  Essential predecessor directives are carried forward. One 154-byte optional
  whitespace section answers a specific question without loading sibling history.
- Ordinary output: 13,327 bytes, correctly refused. Explicit 14,336-byte maximum
  and reason: 13,459 emitted bytes, including the override. Baseline task sources:
  6,516 bytes; mandatory installed rules/schemas: 40,641; raw startup: 47,157.
  Packet route plus fresh task/checkpoint reads: 60,616, with duplicates counted.
- Provisional startup 73,728 bytes derives from the packet route plus 15% headroom
  rounded to 4 KiB. Working reserve 24,576 bytes allocates 8 KiB each for execution,
  verification/results and outgoing checkpoint. A 13,113-byte expansion crosses
  the startup bound; the 154-byte section fits. These are synthetic byte proposals,
  with host overhead, token capacity and actual execution needs unmeasured.
- Checkpoint-history growth 220,150 bytes and ordinary-detail growth 108,001 bytes
  preserve packet/baseline. Ordinary detail changes stale the preview basis but
  refreshed output is byte-identical. Same-timestamp gate/owner changes prevent
  execution. Sixteen synthetic links and selected anchors resolve.
- New shared input is included (14,295-byte packet), requires review and is not
  acknowledged by export. Source edits stale the preview. Required oversized input
  emits 110,297 bytes in preview and refuses export without truncation. Budget
  exhaustion saves a stop checkpoint; exact previous bytes remain in history and
  the input remains pending. No successor is launched.

### Verification and limitations

**153/153 tests and typecheck pass; build passes.** The first sandbox run passed
129 tests and failed 24 localhost tests with `EPERM`; the escalated rerun passed all
153. Synthetic corpus validation: 23 files, zero errors, three intentional warnings
(oversized source/history and waiting checkpoint). Frozen CLI validation with
explicit `--data`: 26/0/0 and 23/0/1, the existing raw-journal warning. All 50 frozen
hashes, original 71 tested-source hashes and 14 protected inventories are unchanged.
Diff, link/anchor, exact rebased archive, handoff size and task/index checks pass.

Runtime gaps are recorded, not silently implemented: receiver eligibility judgment;
whole-corpus preview invalidation on irrelevant detail growth; whole-file exporter
expansion; checklist-anchor-only parsed Evidence/Artifact links; no runtime startup/
reserve controller or host capacity measurement. Planning export permits review-
required snapshots and never grants implementation authority. The authored read
route and harness assertions are not measured agent behavior or human acceptance.

Evidence logs, source manifests, preservation snapshots and exact generated review
artifacts are outside the repository under
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/flight-continuation-20260914/`.
No live KB capture/regen, migration/adoption, policy changes, commit, push, messages
or external changes. Installed protocol/base remain 0.4.17, accomplishment 0.4.18.

T09 stays partial: explicit fresh-session authority, receiver budget calibration,
observed continuation acceptance and separately gated live reconciliation remain.
The [current packet](../next-task.md) retains those gates; no T10 is invented.


## Continuation 2026-09-14: gated authority review

Rechecked the incoming assignment on `main` at
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`. Its explicit no-launch and no-live-change
boundaries remain. This session completes the authorized gate review and chaining
capture, not T09 acceptance. The user's approval question was answered: one isolated
synthetic receiver exercise needs launch authority; live reconciliation/adoption
needs a separately prepared and reviewed concrete change with backup/rollback evidence.

Fresh explicit `--data` validation: frozen corpus 26 files/0 errors/0 warnings;
fixtures 23 files/0 errors/1 existing raw-journal warning. All 50 frozen hashes,
14 protected inventories and 73 previously tested source hashes match the prior
manifests. Prior 153/153 tests, typecheck and build were not rerun for docs only.
Diff, links/anchors, task/index agreement, size, exact archive rebasing and append-only
evidence are checked for this capture. Sequential observations cannot exclude
arbitrary concurrent external edits.

Private records:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/gate-review-20260914-3olv_t_j/`.
The [incoming packet](t09-assignment-20260914-3.md) is uniquely archived with rebased
links. Only T09 handoff/task/index/evidence documentation changed. No agent,
synthetic evaluator, migration, adoption, live capture/regen, commit, push,
message or external-system change occurred.

Next: with explicit launch approval, measure one synthetic fresh-agent continuation,
its actual outcome, selected reads, missed constraints, stale-action prevention,
startup allowance and working reserve. Host capacity is still unmeasured. Live
reconciliation/adoption retains separate gates. Without new authority, report gates
and stop. T09 remains partial, unassigned and final with no successor. The human
starts any next session using the [current packet](../next-task.md).


## Continuation 2026-09-14: gated authority recheck

The renewed chaining request was reviewed on `main` at
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`. The incoming assignment explicitly
requires new authority for an agent launch and separately gated live changes.
This request supplies neither; the authorized gate review and outgoing capture
are complete, while T09 acceptance remains partial. No receiver exercise or
migration was repeated. The handoff's no-live-capture/regen instruction governs
this assignment over CLAUDE.md's general dogfooding instruction.

Fresh validation with explicit `--data`: frozen corpus **26 files/0 errors/0
warnings**; fixtures **23 files/0 errors/1 existing raw-journal warning**.
All **73 previously tested source hashes, 50 frozen hashes and 14 protected
inventories** match the prior review and this session's baseline. Prior
**153/153 tests, typecheck and build** remain historical verification; no code
changed and they were not rerun. Sequential preservation checks do not establish
external-system state or exclude arbitrary concurrent editors.

The [incoming assignment](t09-assignment-20260914-4.md) is uniquely archived with
exactly rebased links. T09, the execution index and the current packet retain the
fresh-session authority, receiver calibration and live reconciliation gates.
Private baseline manifests, validation logs and final verification are at
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/gate-review-20260914-_8a4uqv2/`.
Only handoff/task/index/evidence documentation and the incoming archive changed.
No agent launch, live capture/regen, adoption, commit, push, publication, messages
or external-system change occurred.

Next: after explicit launch authority, run one synthetic receiver continuation
and measure outcome, selected reads, missed constraints, stale-action prevention,
startup capacity and working reserve. Host overhead remains unmeasured. Live
reconciliation/adoption requires its own concrete reviewed change and backup/
rollback evidence. Without new authority, report these gates and stop. T09 stays
partial, unassigned and final with no successor; a human starts the next session
using the [current handoff](../next-task.md).

Final capture verification: **118 local links/anchors** resolve; the incoming
archive is exactly rebased and prior evidence remains append-only. Task/index
agree, `git diff --check` passes, and the outgoing file is **6,126 UTF-8 bytes**,
within the 6 KiB target. This file measurement does not establish session capacity
or receiver acceptance. Final results are retained in private `final-verification.json`.


## Continuation 2026-09-14: renewed gate recheck

The current chaining request was reviewed on `main` at
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`. Repository identity and resolved
paths agree with the incoming assignment. The environment still selects the
frozen corpus. No new authority for a fresh-agent launch or live changes was
provided; the assignment explicitly requires reporting these gates and stopping.
Its no-live-capture/regen boundary governs over CLAUDE.md's general dogfooding
instruction. This review completes the authorized verification and outgoing
capture, not T09 acceptance. No receiver exercise or migration was repeated.

Fresh validation with explicit `--data`: frozen corpus **26 files/0 errors/0
warnings**; fixtures **23 files/0 errors/1 existing raw-journal warning**.
All **73 tested source hashes, 50 frozen hashes and 14 protected inventories**
match the preceding review and this session's baseline. Prior **153/153 tests,
typecheck and build** remain historical; no code changed and they were not rerun.
Sequential observations do not establish external-system state or exclude
arbitrary concurrent editors.

The [incoming assignment](t09-assignment-20260914-5.md) is uniquely archived with
rebased links. Prior evidence remains append-only. T09, the execution index and
[current handoff](../next-task.md) retain the fresh-session acceptance, receiver
calibration and separately gated live reconciliation requirements. Private
baselines, validation logs and final checks are in
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/gate-review-20260914-t9t4005y/`.
Only these four documentation files and the incoming archive changed in the repo.

Next: with explicit new agent-launch authority, run one synthetic receiving
session and measure outcome, selected reads, missed constraints, stale-action
prevention and usable startup capacity/working reserve. Host overhead remains
unmeasured. Live reconciliation/adoption requires separate reviewed authority
and the existing identity, contract, backup and rollback gates. Without new
authority, report the gates and stop. T09 remains partial, unassigned and final,
with no successor; the human starts the next session. No agent launch, live
capture/regen, adoption, commit, push, publication, messages or external change
occurred.

Final capture checks: **120 local links/anchors** resolve; archive rebasing is
exact and prior evidence is preserved. Task/index agree; `git diff --check`
passes. The outgoing file is **6,123 UTF-8 bytes**, within the 6 KiB target;
this does not measure total session capacity or receiver acceptance. Results
are retained in private `final-verification.json`.


## Continuation 2026-09-14: chaining gate verification

Reviewed the incoming assignment on `main` at
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`; repository identity and resolved
paths match. The chaining request provides no new fresh-agent or live-change
authority. The assignment's explicit stop gate applies, including its prohibition
on live capture/regen over CLAUDE.md's general dogfooding instruction. This session
completes the permitted verification and outgoing capture; T09 remains partial,
unassigned and final, with no successor.

Fresh CLI validation with explicit `--data` passes: frozen corpus **26 files,
0 errors, 0 warnings**; fixtures **23 files, 0 errors, 1 existing raw-journal
warning**. All **73 tested source hashes, 50 frozen hashes and 14 protected
inventories** match the preceding review and entry baseline. Prior **153/153 tests,
typecheck and build** are historical and were not rerun for documentation changes.
Sequential preservation checks cannot exclude arbitrary concurrent editors or
establish current external-system state.

The [incoming assignment](t09-assignment-20260914-6.md) is uniquely archived with
rebased links; exact original bytes and verification artifacts are retained at
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/gate-review-20260914-vv967esh/`.
Only T09's current handoff, task, index, append-only evidence and new archive are
changed in the repository. The [current handoff](../next-task.md) retains essential
constraints and the exact next-session prompt. No migration or synthetic exercise
was repeated; no agent launch, live adoption/capture/regen, commit, push,
publication, message or external change occurred.

Next: explicit new agent-launch authority permits one synthetic receiving session
to measure successful continuation, selected reads, missed constraints, stale-action
prevention and usable startup capacity/working reserve, with host overhead reported
separately. Live reconciliation/adoption requires separate reviewed authority and
existing identity, contract, backup and rollback gates. Without that authority,
report the gates and stop. The human starts the next session.

Final capture checks: **122 local links/anchors** resolve; archive rebasing is
exact and prior evidence is preserved. Task/index agree; `git diff --check`
passes. The outgoing packet is **6,129 UTF-8 bytes**, within the 6 KiB target.
This file size does not measure host capacity or receiver acceptance. Final
results are retained in private `final-verification.json`.


## Continuation 2026-09-14: acceptance gate recheck

Reviewed the renewed chaining request on `main` at
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`; repository identity and resolved
paths match the incoming assignment. No new agent-launch or live-change authority
was supplied. Its explicit stop gate applies, including no live capture/regen
over CLAUDE.md's general dogfooding instruction. The authorized verification and
outgoing capture are complete; T09 remains partial, unassigned and final with no
successor. Fresh-session acceptance and receiver capacity remain unmeasured.

Fresh CLI validation with explicit `--data`: frozen corpus **26 files, 0 errors,
0 warnings**; fixtures **23 files, 0 errors, 1 existing raw-journal warning**.
All **73 tested source hashes, 50 frozen hashes and 14 protected inventories**
match the preceding review and this session's baseline. Prior **153/153 tests,
typecheck and build** remain historical; no code changed and they were not rerun.
Sequential observations cannot exclude arbitrary concurrent edits or establish
current external-system state.

The [incoming assignment](t09-assignment-20260914-7.md) is uniquely archived with
rebased links. Exact incoming bytes, baseline manifests, validation logs and final
checks are retained outside the repository at
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/gate-review-20260914-7k5hc47k/`.
Prior evidence remains append-only. Only T09's current handoff, task, execution
index, evidence and new incoming archive changed in the repository. No agent
launch, synthetic exercise, migration, live adoption/capture/regen, commit, push,
publication, message or external-system change occurred.

Next: with explicit new agent-launch authority, run one synthetic receiving
session and measure outcome, selected reads, missed constraints, stale-action
prevention, usable startup capacity and working reserve; report host overhead
separately. Live reconciliation/adoption requires separate reviewed authority
and the existing identity, contract, backup and rollback gates. Without new
authority, report the gates and stop. The human starts the next session with the
[current handoff](../next-task.md).

Final capture checks: **124 local links/anchors** resolve; incoming archive
rebasing is exact and prior evidence is preserved. Task/index agree and
`git diff --check` passes. The outgoing packet is **6,126 UTF-8 bytes**, within
the 6 KiB target; this does not measure host capacity or receiver acceptance.
Results are retained in private `final-verification.json`.


## Continuation 2026-09-14: gate review cap

The user requested a counter and cap for repeating this handoff step. Set the
completed gate-only review counter to **5** and its cap to **5**, counting the
recorded gated authority review, gated authority recheck, renewed gate recheck,
chaining gate verification and acceptance gate recheck. Five is the agent-selected
cap; the user requested the control without specifying a number. This policy edit
does not increment the counter or claim T09 acceptance.

The cap is already reached. With unchanged authority/evidence, stop before
validation or capture: report the blocker without another archive, rewrite or
chaining prompt. This rule explicitly overrides the outgoing capture checklist.
New authority/evidence permits only the substantive work it unlocks and does not
reset the counter. Only the user may reset/increase the cap; below cap, each
gate-only review increments it once in outgoing capture. T09 remains partial.

The [incoming packet](t09-assignment-20260914-8.md) is archived with rebased links;
prior evidence is preserved. The [current handoff](../next-task.md), task and index
agree on the stop rule. Verification artifacts are outside the repository at
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/gate-cap-20260914-fn172qk7/`.

Verification: both explicit-data frozen validations pass (26/0/0 and 23/0/1,
the known raw-journal warning). All 73 tested source hashes, 50 frozen hashes
and 14 protected inventories are unchanged. All 126 local links/anchors resolve;
archive rebasing, append-only evidence, task/index agreement and diff checks pass.
The packet is 6,136 UTF-8 bytes, within the 6 KiB target. Counter initialization
matches the five recorded reviews; the cap overrides repeated capture. Code is
unchanged, so prior 153-test/typecheck/build results remain historical. No live
capture/regen, agent launch, commit or external changes. Preservation observations
remain sequential and do not exclude arbitrary concurrent editors.


## Continuation 2026-09-14: fresh synthetic receiver

The user explicitly authorized one fresh synthetic receiver, result verification
and outgoing capture; live reconciliation/adoption remained unauthorized. This
new authority unlocked substantive work without resetting/incrementing the **5/5**
gate-only counter. One receiver launched without parent conversation history.
It selected verification from flight/task/current T3, saved **13 passing synthetic
assertions**, and preserved source/lifecycle bytes. An outer display truncation
required recovery reads. The parent later interrupted/resumed the same session;
a usage-limit error stopped it before outgoing capture or final report. No second
agent launched. This is partial receiver evidence, not uninterrupted acceptance.

The [receiver evaluation](../../examples/flight-continuation/receiver-evaluation.md)
records 67,771 logged source bytes; 58,526 generated tool-output bytes versus
17,783 wrapper-emitted bytes; a concurrent logger filename collision; incomplete
working-reserve accounting; and unknown exact delivered context/host capacity.
Zero old-checkpoint/release-body reads appear in the source trace. The receiver
never reached stale/input/reserve probes or checkpoint capture. Quota failure is
not context-exhaustion evidence; no calibrated admission or production-search
acceptance is claimed.

After preserving the stopped receiver, parent reconciliation found unchanged KB
sources and only complete initialization transactions. Parent independently
verified results, captured a waiting baseline plus oversized-input stop checkpoint
through the supported writer, and verified exact prior history. Parent-run owner,
dependency, input, overflow and growing-history probes pass; these remain separate
from receiver performance. The parent baseline packet is 14,334 bytes under an
explicit 14,336-byte maximum/reason. All six synthetic corpora have zero validation
errors. Task status/checklists and input dispositions remain unchanged.

Fresh **153/153 tests/typecheck and build pass**; 24 localhost EPERM failures in the
first run were resolved by the escalated run. The existing flight evaluator passes.
Explicit frozen validation is **26/0/0** and **23/0/1**, the known raw-journal warning.
All **73 tested source hashes, 50 frozen hashes and 14 protected inventories** are
unchanged. Runtime, schemas and protocol are unchanged. Sequential preservation
checks cannot exclude arbitrary concurrent editors or establish live state.

The [incoming packet](t09-assignment-20260914-9.md) is uniquely archived with
rebased links; prior evidence remains append-only. Task, index and
[current handoff](../next-task.md) retain T09 as partial, unassigned and final with
no successor. The one-shot grant is consumed. Next authorized receiver work should
use bounded mandatory reads, unique logger filenames, selected writer guidance
and complete reserve accounting, then finish receiver capture and stale/input/
reserve probes. Those changes are proposed, not verified defaults. Without new
authority/evidence, the 5/5 cap requires stopping before checks or capture.

Private artifacts and final verification:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/fresh-receiver-20260914-y8hdz3y1/`.
No live adoption/reconciliation/capture/regen, private subagent input, commit, push,
publication, messages or external change. Human starts any next session from the
current handoff; a generic chaining request supplies no new launch authority.

Final capture verification: **145 local links/anchors** resolve; incoming archive
rebasing is exact and prior evidence remains append-only. Task/index agree;
`git diff --check` passes. The outgoing packet is **6,143 UTF-8 bytes**, within
the 6 KiB target. This file measurement does not establish receiver capacity or
complete acceptance. Final results are in private `final-verification.json`.
