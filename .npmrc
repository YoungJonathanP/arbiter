# This repo has no private packages; pin the public registry so an install on a
# machine with a corporate mirror cannot bake an unreachable URL into the lockfile.
registry=https://registry.npmjs.org/
