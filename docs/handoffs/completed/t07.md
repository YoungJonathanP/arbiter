# T07 completed: bounded discovery and consistent navigation

Verified 2026-09-08 on `main`, base HEAD
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`. The incoming worktree was clean;
T07 source, tests and documentation remain uncommitted. No commit, push,
publication or messages were sent. Incoming assignment preserved at
[t07-assignment.md](t07-assignment.md). Next assignment: [T08](../../improvement-tasks/08-knowledge-evidence.md).

## Outcome and files

- `src/core/discovery.ts`: shared bounded search over current visible projections;
  selected tiers, literal terms, explicit archives, directory/status/parent filters,
  stable order, previews, totals and next-page metadata. Only selected task rows
  assess their current checkpoint; missing external observations require review.
- `src/cli/args.ts`, `main.ts`: `search` text/JSON surface with validated options.
  Legacy index query outputs remain compatible. `src/web/server.ts` uses the same
  results for search, directory lists and goal/task children, with agent JSON.
  Parent affiliation remains distinct from blocked-by links and start predicates.
- `src/core/dashboard.ts`: task/goal groups now have the existing five-entry
  record limit and truthful overflow. Sidenav groups also have five entries and
  routes to complete non-archived lists, including nested work.
- `src/web/server.ts`: completed checklist/result rows are muted; explicit
  `~~superseded prose~~` retains deletion markup. Directory pages are labeled lists.
- `src/web/handoff.ts`: selected sources and their input-purpose reasons are
  inspectable beside exact previews. Explicit expansion uses the unchanged core
  handoff reconstruction, privacy, CAS and size contracts.
- Bootstrap protocol/base 0.4.13, concrete types 0.4; updates in assets, grammar,
  CLI identity checks and validation/web version gates. Older accepted versions
  remain supported, including 0.4.12. No installed contract edits.
- README, operating skill, [decision and usage](../../v0.4.13-discovery.md), task,
  execution index, successor packet and completion history document the result.
  T08 registration and reporting remain separate work.

## Verification

- Full `npm test`: **95/95 passed**, including TypeScript. Log:
  `/tmp/arbiter-t07-final-tests.log`. Tested source/assets/tests/skill/grammar manifest:
  `sha256:47d9f636708478c49508b705031326f9036c9c342c976d1052cd77f46a567ab8`
  (`/tmp/arbiter-t07-tested-sources.json`; base HEAD plus uncommitted changes).
- Final preview-window, muted-title and child-filter-link refinements: focused
  discovery/handoff suite **18/18 passed**. Log:
  `/tmp/arbiter-t07-final-focused.log`. `npm run build` passed on final source.
- `git diff --check`: passed. Operating skill validator: **Skill is valid!**,
  using `/tmp/arbiter-t05-skill-venv/bin/python`. System Python lacked PyYAML;
  the existing validator environment resolved this without installing packages.
- Source CLI `validate` with explicit `--data`: in-repo corpus **26 files,
  0 errors, 0 warnings**; fixtures **23 files, 0 errors, 1 existing raw warning**
  (`journal/prod-timeout-idea.md`). SHA-256 comparison asserted **all 50 files
  unchanged**, including non-Markdown files; baseline at
  `/tmp/arbiter-t07-frozen-hashes.json`.
- Acceptance tests use temporary synthetic KBs: 123 tasks across multiple pages;
  archived decision found by body text; private owner/doc and history exclusion;
  goal-task child parity; private-parent orphan reachability; stable ties and exact
  counts; current tier-1 search despite stale stored dashboard; bounded long-line
  previews; identical CLI/web JSON; done versus superseded HTML; selected handoff
  expansion with purpose and exact bytes while other inputs/linked bodies remain
  omitted. Existing staging, recovery, parser and export protections still pass.
- Initial HTTP test failures were sandbox localhost EPERM; authorized reruns
  passed. Early checks also caught the old version/archive assertions and a child
  staged-count regression, all corrected. No failing check remains.

## Limits and decisions

Search scans current visible files internally. Its output is bounded, but internal
I/O and sorting scale with corpus size. Pages are fresh reads, so concurrent edits
can change totals/order between requests. No persistent FTS index, atomic cursor or
new canonical type registry was added. Normal discovery omits checkpoint history,
proposals, transaction journals, schemas and protocol files. Existing private-token
redaction cannot detect arbitrary paraphrases. Item source views remain complete;
only related result lists and previews are bounded.

T08 must settle the existing model/corpus/creation/cards/web registration tables
together and define durable decision/finding supersession and evidence. Inline
strikethrough does not establish a machine-readable record supersession relation.
No report or accomplishment-promotion pipeline is implemented yet.

`ARBITER_DATA` still selects the frozen in-repo corpus; `../arbiter-data` is absent.
Live session capture/regen was unavailable, so completion is recorded here. The
user-supplied comparison snapshot remains unsupported protocol 0.4.7, unmodified
and unadopted; its path and compatibility result remain in [T06 evidence](t06.md).
No Finance Hub, USB, transcript, installed-KB or shell-profile changes.

T07 is complete in the task/index. T08 readiness was rechecked against source:
registration remains hardcoded in model/classifier/corpus, facts, creation/checks,
dashboard definitions and web labels/routes; `args.ts` exposes no report command.
The [current handoff](../next-task.md) carries T08 only (6144 UTF-8 bytes; links and
task/index agreement verified). Archive links were rebased for their new directory. Human starts the next session.
