# T04 completion — parser recovery with source diagnostics

Completed and verified 2026-09-07 in YoungJonathanP/arbiter, branch main,
HEAD `4a5e0979ec078cf492df3050b6fdc46196727093`. T01–T04 changes remain
uncommitted. No commit, push, publication or external message was made.
The [incoming assignment](t04-assignment.md) is preserved with its two relative
links rebased to the archive location; assignment text is unchanged.

## Outcome

Checklist, Artifacts, Evidence and Detail docs now recover per entry. Valid
neighbors retain structured navigation and facts; malformed lines and internal
blanks retain source order, indentation and trailing spaces. Repeated
normalization/serialization preserves the content and reaches a fixpoint.
Annotations are supported inside labels, doc titles and step text before anchors;
trailing annotations remain opaque with an explicit repair diagnostic.

Validation and normalization locate malformed input by its 1-based original line,
including frontmatter and blank offsets, and name the expected production.
Broken parsed paths/anchors have a distinct diagnostic code and source line.
The renderer displays valid links and escaped opaque material in order, with
expected syntax and repair advice. Original coordinates survive privacy filtering
via source-line maps; filtered frontmatter still supplies public execution status.

Arbitration rejects append input containing opaque residue, preserves malformed
material while applying valid appends/marks, and verifies parsed effects.
Deduplication and postconditions ignore source coordinates. Unsupported ops remain
staged whole; an opaque apparent link cannot satisfy a structured operation.

## Files and contract

- AST/parser/order/equality: `src/core/model.ts`, `parse.ts`, new `section-rows.ts`,
  `equal.ts`, `serialize.ts`; existing structured arrays remain available.
- Diagnostics and consumers: `normalize.ts`, `validate.ts`, `arbitrate.ts`,
  `corpus.ts`, `projection.ts`, `visibility.ts`, `src/web/server.ts`.
- CLI: `src/cli/main.ts` emits diagnostic line locations; `data.ts` accepts
  0.4.10 and prior supported versions. Bootstrap grammar/protocol/base schema
  advance to 0.4.10; concrete field schemas stay 0.4. README and base type guidance
  describe accepted syntax. [Decision and limits](../../v0.4.10-parser-recovery.md).
- Regressions: new `test/parser-recovery.test.ts`; mixed-section assertion in
  `test/safe-writes.test.ts`; bootstrap/legacy checks in `test/cli.test.ts`.
  Existing conformance corpus and expected canonical bytes remain unchanged.

## Evidence

- `npm test`: **68/68 pass**, including typecheck, canonical gates, recoverable
  writes and localhost renderer requests. New regressions exercise ten recognized
  artifact entries around an annotated malformed line, recovered docs/checklists,
  blank-preserving continuations, four repeated round trips, exact CLI/core/UI
  diagnostics, missing path/anchor distinction, op deduplication, rejected opaque
  appends, and privacy filtering of both body and frontmatter without status loss.
- Localhost tests initially hit sandbox `listen EPERM`; the approved escalated
  run exercised them. A first source-placeholder approach failed the existing
  public-status privacy regression; the final source-line map passes it.
  Bootstrap expectation was updated to the intentional 0.4.10 contract.
- `npm run build`: pass. `git diff --check`: pass.
- Source CLI validation with explicit absolute `--data`, read-only:
  `arbiter-data`: **26 files, 0 errors, 0 warnings**;
  `fixtures/arbiter-data`: **23 files, 0 errors, 1 existing raw-entry warning**.
- SHA-256 manifest comparison of every file under both frozen corpus roots:
  unchanged from session start. `git diff --exit-code HEAD -- arbiter-data fixtures`
  also passes. All corpus mutations used temporary synthetic data.

## Interfaces and limits

`rawRows` addresses structured arrays by index and records opaque/blank rows.
`orderedRows` supports in-place entry edits and appends; future removal/reordering
must update row indices. Entry/continuation `rawLine` refers to the input snapshot;
reparse after writes. `CorpusFile.sourceLines` optionally maps filtered line indices
to original coordinates; `parseItemFile(text, sourceLines)` accepts that map.
Filtered raw previews may have fewer lines than canonical source files.

A malformed checklist line ends continuation attachment until a new valid step;
blanks alone do not. Duplicate headings stay separate and ambiguous arbitration
append targets stay rejected. The existing LF and inter-section separator rules
remain; this is not whole-file byte identity for noncanonical input. Raw human
files retain warning-only validation; HTTP(S) links are not checked over the
network. Literal privacy filtering cannot classify arbitrary paraphrased secrets.
T02 transaction snapshots stay canonical recovery data, excluded from exports.

Live capture is unavailable: ARBITER_DATA still selects the frozen in-repo corpus
and the live `../arbiter-data` directory is absent. CLAUDE.md and the local Arbiter
capture skill were checked. This record captures completion pending a selected
live KB; no fallback writes were made into either frozen corpus.

## Successor readiness

T05 is ready/unassigned. Reinspection of its packet, `docs/task-handoffs.md`, the
manual template, AST/schema and bootstrap contract confirms no current-checkpoint
file role, storage path or replacement lifecycle is implemented. It must choose
that representation first, then specify readiness independently from execution
status and sticky review metadata. Size/export policies remain proposals. Operating
skill changes require the applicable skill-editing workflow after the contract
settles. No T05 implementation was started. The [current handoff](../next-task.md)
assigns T05; a human starts the next session.
