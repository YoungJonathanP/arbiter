# T09: finish migration and continuation evaluation

## Assignment

Complete [T09](../../improvement-tasks/09-migration-evaluation.md) only. Status: partial; owner unassigned; migration readiness review-required.
T09 is last in the [index](../../improvement-plan.md); no successor. Deliver evidenced migration/continuation acceptance, preserving human
control, stable refs and history. Do not execute the external project's work.

## Authority

Read `CLAUDE.md`, T09, and the relevant sections of `docs/task-handoffs.md` and
[template](../../templates/task-handoff.md). Recheck repository identity/branch/HEAD
and dirty state. At 2026-09-09: YoungJonathanP/arbiter, main, base HEAD
`bfdc5519cc730fb0e23bb840b5ac83d6c7bddb66`; T07/T08/T09 are uncommitted.
Preserve all prior changes. Ask before committing per CLAUDE.md; no push, publish,
messages, Finance Hub/ticket/transcript/profile edits or implicit installed upgrade.

User correction, 2026-09-09: the live KB IS
`/Users/jonathanyoung/Documents/arbiter-data-2026-09-06-jonathan-young-nhw4ydwv2p`.
This supersedes previous snapshot-only identity descriptions. Resolve the KB to a receiver-local path.
The shell's ARBITER_DATA still selects frozen in-repo data, and ../arbiter-data is
absent. Always select the confirmed KB explicitly. Saved observations do not verify current external state.

## Verified state

107/107 tests, build and diff checks passed; all 50 frozen-corpus files unchanged.
Source CLI validation: 26/0/0 and 23/0/1 (existing raw journal warning).
Evidence: [partial T09 record](t09.md), optional history.
`src/core/migration.ts` and `test/migration.test.ts`: [usage/limits](../../examples/migration/README.md).

Local private evidence directory:
`/Users/jonathanyoung/Documents/arbiter-t09-local-review/`.
- `REPORT.md`, `selection.json`, `migration-manifest.json`: 16 selected files,
  117,368 scanned bytes, 103 anchors and 119 retained file/anchor mappings.
  36 selected links resolve; 45 remain uninspected; 88 external URLs unverified.
- `selected-assignment.md`: 4,657-byte standalone planning handoff, not an
  installed checkpoint or implementation export. No older handoff/sibling needed
  for planning. Specific conflicts and required-read sections are carried inline.
- `contract-review.diff`: substantive installed/bootstrap differences.
- `backup-before-migration/`, `backup-verification.json`,
  `restore-verification.json`: all 730 files, including Git, copied and restored
  byte-exact in an isolated rehearsal. Source has no transactions directory.

Live KB unchanged, clean master at `724775a83b2996c65487e7de897e2adc75452533`;
270 supplied hashes match. Protocol 0.4.7/base 0.4.6 is unsupported (doctor exit 2).
No live migration/capture/regen or fresh-agent exercise occurred. Synthetic checks
cover recursive history, stale gates, owners, scope and rollback; a 9,969-byte
packet stays constant with 1.1 MB added history. Actual agent behavior is unmeasured.

## Constraints and gates

- Review actual contract differences in the private diff: projection, review,
  CAS, checkpoint/history, owner/UUID, discovery and knowledge schemas. Never
  relabel versions or overwrite installed rules implicitly. Adoption is not authorized.
- Select the migration working copy explicitly before migration. Live identity confirmation does not approve a contract upgrade.
  Backups are verified baselines; recheck hashes and preserve concurrent edits.
- Preserve the live KB, USB and both frozen corpora. Keep employer content and
  detailed manifests outside the shareable repository; synthetic fixtures only.
- Confirm accountable owner and scope/status/gate resolutions from evidence.
  A newer timestamp does not settle contradictions; superseded is not completed.
- The separate local case-study path or waiver is still pending (asked this
  session). Never substitute frozen data or this newly generated review for it.
- Isolated fresh-agent execution needs authorization per T09. Do not treat unit
  tests as fresh sessions or claim live external verification from saved records.
- Stable IDs/anchors and exact history bytes must survive; unresolved links block
  complete acceptance. Retain `.arbiter/transactions` in any future backup.
- Use existing CAS/staging/checkpoint writer for supported capture. Preserve
  immutable history and re-observe after partial writes. No multi-file atomicity.
  Normal bootstrap excludes history; 6 KiB target/10 KiB whole-packet ceiling,
  larger budget/reason explicit. Offline exports remain planning-only until live
  gates are checked. Never truncate constraints or override authority/privacy.

## Next actions

1. Recheck workspace/source/backup hashes. Obtain case-study path/waiver; inspect
   selected private report/manifest/diff sections for a concrete adoption decision.
2. Resolve contract adoption, working-copy selection and named owner/scope/gates
   before dependent mutation. Finish targeted old-link checks. If authorized,
   migrate on that copy and verify references, history and rollback bytes.
3. If authorized, run isolated fresh-agent exercises; measure continuation,
   context, unnecessary reads, missed constraints and stale-action prevention.
   Otherwise retain missing evidence and stop.

## Completion and outgoing capture

All T09 acceptance criteria need actual evidence before completion. Run npm test
(escalate localhost sandbox failures), npm run build and git diff --check after
code changes; source CLI validate both frozen corpora with explicit --data and
assert expected outcomes/hashes. Use skill-creator for skill edits if needed.
Update T09/index and evidence honestly. Archive incoming packets uniquely with
rebased links. Use the template; verify links, size and task/index agreement.
If gated, retain T09 progress/gates/next actions here; after full completion replace
this file with completion/no successor.

Return this file link and the prompt:
"Open docs/handoffs/next-task.md in the Arbiter checkout.
Complete its current assignment and, after verification, create the chaining
handoff for a new agent exactly as instructed."
End; the human starts the next session.
